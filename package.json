{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "scripts": {
    "package": "lerna add $FIRST --scope=$SECOND",
    "package:dev": "lerna add $FIRST --scope=$SECOND --dev",
    "lerna": "lerna"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}